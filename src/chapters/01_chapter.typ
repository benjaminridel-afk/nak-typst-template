= Showing off this template
== Citing
#lorem(50) @nocera_software_2023

#lorem(30) @nocera_software_2023 @moskala_android_2017

== Code blocks

=== Inline code
Adding `rbx` to `rcx` gives
the desired result. Let's also use abbreviations like @cpu, a fancy @cfg or @oop that many love and hate at the same time. And if we use the abbreviation @oop a second time, it looks like this.

What is ```rust fn main()``` in Rust
would be ```c int main()``` in C.

=== Code block
This is an example for a simple code block:
```rust
fn main() {
    println!("Hello World!");
}
```
